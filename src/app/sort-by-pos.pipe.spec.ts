import { SortByPosPipe } from './sort-by-pos.pipe';

describe('SortByPosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPosPipe();
    expect(pipe).toBeTruthy();
  });
});
